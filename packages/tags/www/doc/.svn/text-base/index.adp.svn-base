<master>
  <property name="title">Tagging</property>
  
<p>Tagging is very useful (and fashionable).
</p>
<p>The <font face="courier new,courier,monospace">tags</font> package allows
tagging against <font face="courier new,courier,monospace">acs_objects</font> and to this end it defines the <font face="courier
new,courier,monospace">tags_tags</font> table with a foreign key constraint on <font face="courier new,courier,monospace">acs_objects</font>.
</p>
<p><font face="courier new,courier,monospace">tags</font> acts much like general-comments, attachments and ratings, providing to the programmer a way to include tagging in any application.
</p>
<p>To generate a link in your pages to add a tag to an object you can invoke <font face="courier new,courier,monospace">tags::create_link</font>. This&nbsp; brings you to the <font face="courier new,courier,monospace">edit</font> page, where you can view, add or change your own object's tags. The <font face="courier new,courier,monospace">edit</font> page shows also the other object's tags and the popular tags, that you can click to add to the form before submitting.
</p>
<p>The 'Popular tags' portlet shows the tags as a tagcloud. You can include it in your pages:
  <br />
</p>
<ol>
  <li>via <font face="courier new,courier,monospace">&lt;include src="/packages/tags/lib/tagcloud"&gt;</font> in your adp</li>
  <li>or via <font face="courier new,courier,monospace">{{adp portlets/tagcloud}}</font>&nbsp; after copying the portlet from the <font face="courier new,courier,monospace">resources</font> folder to the portlets folder of <font face="courier new,courier,monospace">xowiki</font></li>
</ol><font face="courier new,courier,monospace"></font>Clicking a tag finally brings you to the tagged-items page, where you can view, order and filter the tagged items.
<p>Actually the package is marked as a singleton and is localized only in English and Italian.
</p>
